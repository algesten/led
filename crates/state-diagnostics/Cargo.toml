[package]
name = "led-state-diagnostics"
version = "0.0.0"
edition = "2024"
publish = false

# Diagnostic ABI shapes shared between `driver-lsp/core` (producer)
# and `driver-terminal/{core,native}` (renderer). The per-buffer
# diagnostic roster + per-server status containers have moved into
# `driver-lsp-core` per the EXAMPLE-ARCH audit; this crate now holds
# only the wire-shape `Diagnostic` / `DiagnosticSeverity` so the
# terminal driver can paint severities without taking a forbidden
# driver-to-driver dependency on `driver-lsp-core`.

[dependencies]
